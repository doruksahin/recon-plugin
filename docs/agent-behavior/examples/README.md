# Evidence example catalog

Examples are optional calibration material. Read only the example closest to
the current claim; never treat an example as a substitute for the current
task's evidence.

| Example | Use when |
| --- | --- |
| [Recorded repro runtime](recorded-repro-runtime.md) | Distinguishing fixture-proven rail properties from a live task claim and broader portability/generalization claims |

Each audit must expose missing evidence as clearly as passing evidence. Add a
new example only from a real repository change with durable source records.
